Prerequisites:
1-Java and Atlassian SDK configurations -- https://developer.atlassian.com/server/framework/atlassian-sdk/getting-started-tutorial-faq/
2-A running local Jira

Steps to Install this plugin:
1-If you have made changes to the source, be sure to first run atlas-package on the folder
2-Install bat is configured as follows;
call atlas-mvn package -o -DskipTests
call atlas-install-plugin --username *login name* --password *login name's password* --plugin-key com.example.plugins.tutorial -p *jira port number* --server *localhost(can be just localhost)* --context-path 
3-You may need to atlas-mvn package and run install.bat again
4-Run "install.bat" on cmd
5-Open your jira and check add-ons


Full documentation is always available at:

https://developer.atlassian.com/display/DOCS/Introduction+to+the+Atlassian+Plugin+SDK
